name = "shipyard-github-oauth"
main = "src/index.ts"
compatibility_date = "2025-01-01"
account_id = "191909c2c1a28f6cb73d12e3362b874c"  # Jacob@schoolai.com's Account

# Development settings
[dev]
port = 4445
local_protocol = "http"

# Environment variables (non-secret)
# Default to production since that's what the deployed worker should be
[vars]
ENVIRONMENT = "production"

# Development environment overrides (use: wrangler dev --env development)
[env.development.vars]
ENVIRONMENT = "development"

# Secrets (set via wrangler secret put):
# - GITHUB_CLIENT_ID
# - GITHUB_CLIENT_SECRET
