# =============================================================================
# Session Server Environment Variables
# =============================================================================
# For local development, copy values to .dev.vars (wrangler's local secrets file):
#   cp .env.example .dev.vars
#
# For production, use: wrangler secret put SECRET_NAME

# -----------------------------------------------------------------------------
# GitHub OAuth App Credentials
# -----------------------------------------------------------------------------
# Create at: https://github.com/settings/developers
# Callback URL: http://localhost:4444/auth/device/verify (dev)
#              https://shipyard-session-server.jacob-191.workers.dev/auth/device/verify (prod)
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# -----------------------------------------------------------------------------
# JWT Signing Secret
# -----------------------------------------------------------------------------
# Must be at least 32 characters. Generate with:
#   openssl rand -base64 32
JWT_SECRET=your-jwt-secret-at-least-32-characters-long
