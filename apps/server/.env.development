# Local development state - all logs/data in repo for easy monitoring
# This file is auto-loaded when LOG_LEVEL or other env vars suggest dev mode

# Redirect all Shipyard state (logs, persistence, etc.) to repo
SHIPYARD_STATE_DIR=.dev-state

# Redirect Claude projects dir to repo
CLAUDE_PROJECTS_DIR=.dev-state/claude-projects

# Enable debug logging
LOG_LEVEL=debug

# Minimum epoch for data sync - bump this to force all clients to reset
# All clients with epoch < this value are rejected and must clear storage
SHIPYARD_EPOCH=1
