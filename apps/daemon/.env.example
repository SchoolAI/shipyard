# =============================================================================
# Daemon Environment Variables
# =============================================================================
# Copy this file to .env and fill in real values:
#   cp .env.example .env

# -----------------------------------------------------------------------------
# Anthropic API Key
# -----------------------------------------------------------------------------
# Required for running Claude agent tasks. Optional if you only need
# signaling/presence (the daemon will connect to the session server but
# won't be able to spawn agents without this).
ANTHROPIC_API_KEY=sk-ant-xxx

# -----------------------------------------------------------------------------
# Signaling Server Connection
# -----------------------------------------------------------------------------
# The URL must include your user ID in the path. The user ID in the URL
# MUST match the `sub` claim in your JWT token (e.g. both must be "gh_12345").
#
# To find your GitHub user ID:
#   curl -s https://api.github.com/users/YOUR_USERNAME | jq .id
#
# To generate a JWT token, use the helper script:
#   pnpm tsx scripts/generate-daemon-token.ts --user-id YOUR_GITHUB_ID --username YOUR_GITHUB_USERNAME
#
# The script reads JWT_SECRET from apps/session-server/.dev.vars automatically.

# Local development:
SHIPYARD_SIGNALING_URL=ws://localhost:4444/personal/gh_YOUR_GITHUB_ID
SHIPYARD_USER_TOKEN=YOUR_JWT_TOKEN_HERE

# Production (after OAuth):
# SHIPYARD_SIGNALING_URL=wss://shipyard-session-server.jacob-191.workers.dev/personal/gh_YOUR_GITHUB_ID
# SHIPYARD_USER_TOKEN=YOUR_PRODUCTION_JWT

# -----------------------------------------------------------------------------
# Machine Identity
# -----------------------------------------------------------------------------
# Defaults to os.hostname() if not set.
# SHIPYARD_MACHINE_ID=
# SHIPYARD_MACHINE_NAME=

# -----------------------------------------------------------------------------
# Data Directory
# -----------------------------------------------------------------------------
# Where Loro documents and other persistent data are stored.
# Default: ~/.shipyard/data
# SHIPYARD_DATA_DIR=

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: debug | info | warn | error (default: info)
# LOG_LEVEL=debug
