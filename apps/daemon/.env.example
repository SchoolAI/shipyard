# =============================================================================
# Daemon Environment Variables
# =============================================================================
# Copy this file to .env and fill in real values:
#   cp .env.example .env

# -----------------------------------------------------------------------------
# Dev Mode
# -----------------------------------------------------------------------------
# When set, config and data are stored in ~/.shipyard-dev/ instead of
# ~/.shipyard/. Prevents local dev from overwriting production config.
# Set automatically by dev-all.sh and setup-login.sh.
# SHIPYARD_DEV=1

# -----------------------------------------------------------------------------
# Anthropic API Key
# -----------------------------------------------------------------------------
# Required for running Claude agent tasks. Optional if you only need
# signaling/presence (the daemon will connect to the session server but
# won't be able to spawn agents without this).
ANTHROPIC_API_KEY=sk-ant-xxx

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# Preferred: Run `pnpm setup:login` (or `shipyard login`) to authenticate.
# Token is saved to ~/.shipyard/config.json automatically.
#
# Override: Set this env var to skip device flow (useful for CI/automation).
# SHIPYARD_USER_TOKEN=eyJ...

# -----------------------------------------------------------------------------
# Signaling Server
# -----------------------------------------------------------------------------
# Used for both HTTP endpoints (login) and WebSocket signaling.
# The daemon derives the /personal/{userId} path from your JWT automatically.
# Default: https://shipyard-session-server.jacob-191.workers.dev
# Override for local development:
# SHIPYARD_SIGNALING_URL=http://localhost:4444

# -----------------------------------------------------------------------------
# Machine Identity
# -----------------------------------------------------------------------------
# Defaults to os.hostname() if not set.
# SHIPYARD_MACHINE_ID=
# SHIPYARD_MACHINE_NAME=

# -----------------------------------------------------------------------------
# Data Directory
# -----------------------------------------------------------------------------
# Where Loro documents and other persistent data are stored.
# Default: ~/.shipyard/data
# SHIPYARD_DATA_DIR=

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: debug | info | warn | error (default: info)
# LOG_LEVEL=debug
