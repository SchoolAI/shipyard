# =============================================================================
# Daemon Environment Variables
# =============================================================================
# Copy this file to .env and fill in real values:
#   cp .env.example .env

# -----------------------------------------------------------------------------
# Anthropic API Key
# -----------------------------------------------------------------------------
# Required for running Claude agent tasks. Optional if you only need
# signaling/presence (--serve mode).
ANTHROPIC_API_KEY=sk-ant-xxx

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# Preferred: Run `shipyard login` to authenticate via device flow.
# Token is saved to ~/.shipyard/config.json automatically.
#
# Override: Set this env var to skip device flow (useful for CI/automation).
# SHIPYARD_USER_TOKEN=eyJ...

# -----------------------------------------------------------------------------
# Signaling Server
# -----------------------------------------------------------------------------
# Default: wss://shipyard-session-server.jacob-191.workers.dev
# Override for local development:
# SHIPYARD_SIGNALING_URL=ws://localhost:4444

# -----------------------------------------------------------------------------
# Machine Identity
# -----------------------------------------------------------------------------
# Defaults to os.hostname() if not set.
# SHIPYARD_MACHINE_ID=my-machine
# SHIPYARD_MACHINE_NAME=My MacBook Pro

# -----------------------------------------------------------------------------
# Data Directory
# -----------------------------------------------------------------------------
# Where Loro documents and persistent data are stored.
# Default: ~/.shipyard/data
# SHIPYARD_DATA_DIR=~/.shipyard/data

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: debug | info | warn | error (default: info)
# LOG_LEVEL=debug
