# Development environment defaults
# Committed to git so all developers get same config

# Minimum epoch for CRDT reset system
# Bump this value (1→2→3...) to invalidate all old data
# All peers will be forced to clear IndexedDB and sync fresh
# CRITICAL: Must match apps/server/.env.development MINIMUM_EPOCH value
# Mismatched values allow clients to bypass validation via P2P WebRTC sync
MINIMUM_EPOCH=1
