# Shipyard Daemon Configuration
#
# This file is read when the daemon starts (both direct and via MCP).
# Copy to .env and customize as needed.

# Log level for spawned agents
# Valid values: debug, info, warn, error
# When set to "debug", passes --debug "api,hooks" to spawned Claude Code processes
# This enables detailed API and hook logging for troubleshooting
LOG_LEVEL=info

# Directory where Claude Code stores session files (advanced)
# Note: Use $HOME instead of ~ (tilde won't expand in .env files)
# CLAUDE_PROJECTS_DIR=$HOME/.claude/projects

# WebSocket port for daemon communication
# Each worktree gets a unique port via worktree-env.sh
# Default: 56609 (main worktree)
# DAEMON_PORT=56609

# State directory for lock files and logs
# Each worktree gets an isolated directory
# Note: Use $HOME instead of ~ (tilde won't expand in .env files)
# Default: ~/.shipyard (main worktree)
# SHIPYARD_STATE_DIR=$HOME/.shipyard

# Web app URL for task links in agent prompts
# Set per-worktree to point to the correct Vite dev server
# Default: http://localhost:5173
# SHIPYARD_WEB_URL=http://localhost:5173
