# Shipyard Web Configuration
#
# NOTE: Production URLs are the defaults. Only set these for local development.

# Registry server port for MCP server discovery (optional)
# Default: tries ports 32191, 32192
# VITE_REGISTRY_PORT=32191

# WebRTC signaling server for P2P peer discovery
# Default: wss://shipyard-signaling.jacob-191.workers.dev (production)
# Uncomment for local development:
# VITE_WEBRTC_SIGNALING=ws://localhost:4444

# OAuth endpoint (handled by signaling server at /auth/github/callback)
# Default: https://shipyard-signaling.jacob-191.workers.dev (production)
# Uncomment for local development (same as signaling server):
# VITE_GITHUB_OAUTH_WORKER=http://localhost:4444

# GitHub OAuth Client ID (required for GitHub authentication)
VITE_GITHUB_CLIENT_ID=Ov23liNnbDyIs6wu4Btd

# TURN server for WebRTC (optional, improves NAT traversal)
# TURN servers relay traffic when direct P2P connection fails.
# This is common on mobile networks and corporate firewalls.
# Uncomment and configure if you have a TURN server:
# VITE_TURN_URL=turn:turn.example.com:3478
# VITE_TURN_USERNAME=username
# VITE_TURN_CREDENTIAL=password
