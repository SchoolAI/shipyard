# Port-dependent values (VITE_WEBRTC_SIGNALING, VITE_GITHUB_OAUTH_WORKER)
# are set by worktree-env.sh shell exports, not here.
# This allows each worktree to use different ports.

# GitHub OAuth Client ID (public, safe to commit)
VITE_GITHUB_CLIENT_ID=Ov23liNnbDyIs6wu4Btd
