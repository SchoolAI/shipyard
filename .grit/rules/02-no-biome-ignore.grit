language js

or {
  `// biome-ignore` as $comment,
  `// biome-ignore-start` as $comment,
  `// biome-ignore-all` as $comment
} where {
  register_diagnostic(span=$comment, message="biome-ignore suppressions are not allowed. Fix the underlying issue or disable the rule in biome.json.", severity="error")
}
