{
  "mcpServers": {
    "peer-plan": {
      "type": "stdio",
      "command": "node",
      "args": ["apps/server/dist/index.mjs"],
      "env": {
        "NODE_ENV": "development",
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
