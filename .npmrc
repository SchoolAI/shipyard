# Hoist wrtc platform packages so @roamhq/wrtc can find them
# wrtc looks for platform binaries in ./node_modules/@roamhq/wrtc-{platform}
# but pnpm stores them in .pnpm store by default
public-hoist-pattern[]=*wrtc*

# Force linux x64 glibc architecture for Docker builds
# This ensures @roamhq/wrtc-linux-x64 is installed in Docker (linux/amd64)
supportedArchitectures.os=current,linux
supportedArchitectures.cpu=current,x64
supportedArchitectures.libc=current,glibc
